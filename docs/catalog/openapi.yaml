components:
  schemas:
    AnchorRef:
      additionalProperties: false
      properties:
        external_id:
          type: string
        link_kind:
          description: 0=exact 1=probable 2=related
          format: int32
          type: integer
        matched_by:
          type: string
        source:
          type: string
      required:
        - source
        - external_id
        - link_kind
      type: object
    AnchorTierCell:
      additionalProperties: false
      properties:
        count:
          format: int64
          type: integer
        link_kind:
          description: 0=exact 1=probable 2=related
          format: int32
          type: integer
        source:
          type: string
      required:
        - source
        - link_kind
        - count
      type: object
    CatalogStats:
      additionalProperties: false
      properties:
        anchors_by_source_tier:
          items:
            $ref: "#/components/schemas/AnchorTierCell"
          type:
            - array
            - "null"
        attributions_by_kind:
          items:
            $ref: "#/components/schemas/KindCount"
          type:
            - array
            - "null"
        credits_by_source:
          items:
            $ref: "#/components/schemas/KeyCount"
          type:
            - array
            - "null"
        entities:
          $ref: "#/components/schemas/EntityCounts"
        llm_bid_verdicts:
          items:
            $ref: "#/components/schemas/KeyCount"
          type:
            - array
            - "null"
        queues:
          $ref: "#/components/schemas/QueueLevels"
        source_freshness:
          items:
            $ref: "#/components/schemas/SourceFreshness"
          type:
            - array
            - "null"
        works:
          $ref: "#/components/schemas/WorksMatrix"
      required:
        - works
        - entities
        - credits_by_source
        - attributions_by_kind
        - anchors_by_source_tier
        - queues
        - llm_bid_verdicts
        - source_freshness
      type: object
    CharacterAlias:
      additionalProperties: false
      properties:
        id:
          format: int64
          type: integer
        is_primary_for_locale:
          type: boolean
        kind:
          description: 0=translation 1=spelling_variant 2=search_hint
          format: int32
          type: integer
        lang:
          type: string
        latin:
          type: string
        name:
          type: string
      required:
        - id
        - name
        - lang
        - kind
        - is_primary_for_locale
      type: object
    CharacterDetailResponse:
      additionalProperties: false
      properties:
        aliases:
          items:
            $ref: "#/components/schemas/CharacterAlias"
          type:
            - array
            - "null"
        attr_sources:
          additionalProperties:
            type: string
          type: object
        birthday_day:
          format: int32
          type: integer
        birthday_month:
          format: int32
          type: integer
        blood_type:
          description: 1=A 2=B 3=AB 4=O; absent = unknown
          format: int32
          type: integer
        bust_cm:
          format: int32
          type: integer
        cup:
          type: string
        description:
          type: string
        display_name:
          type: string
        extra:
          additionalProperties: {}
          type: object
        figure_hash:
          type: string
        gender:
          format: int32
          type: integer
        height_cm:
          format: int32
          type: integer
        hip_cm:
          format: int32
          type: integer
        id:
          format: int64
          type: integer
        image_hash:
          type: string
        instance_of:
          format: int64
          type: integer
        intros:
          items:
            $ref: "#/components/schemas/WorkIntro"
          type:
            - array
            - "null"
        lang:
          type: string
        latin:
          type: string
        traits:
          items:
            $ref: "#/components/schemas/CharacterTrait"
          type:
            - array
            - "null"
        waist_cm:
          format: int32
          type: integer
        weight_kg:
          format: int32
          type: integer
      required:
        - id
        - display_name
        - lang
        - aliases
        - intros
        - traits
      type: object
    CharacterHead:
      additionalProperties: false
      properties:
        id:
          format: int64
          type: integer
        latin:
          type: string
        name:
          $ref: "#/components/schemas/NameBuckets"
      required:
        - id
        - name
      type: object
    CharacterTrait:
      additionalProperties: false
      properties:
        group_name:
          description: root-group display name; absent for a root trait
          type: string
        group_tid:
          description: VNDB root-group tid (i1=Hair, i35=Eyes, …); empty when the trait is itself a root
          type: string
        id:
          format: int64
          type: integer
        lie:
          description: presented as true in-story but actually a lie (VNDB flag)
          type: boolean
        name:
          description: verbatim VNDB English trait name
          type: string
        sexual:
          description: the trait belongs to a sexual trait family — consumers gate rendering themselves
          type: boolean
        spoiler_level:
          description: "per-character spoiler grade: 0 none / 1 minor / 2 major"
          format: int32
          type: integer
      required:
        - id
        - name
        - group_tid
        - spoiler_level
      type: object
    CharacterWorkRow:
      additionalProperties: false
      properties:
        kind:
          description: "roster appearance strength: 0=unknown 1=main 2=secondary 3=appears (0 also when reached only via a voice credit)"
          format: int32
          type: integer
        spoiler:
          description: "roster appearance spoiler level: 0=none 1=minor 2=major (0 also when reached only via a voice credit)"
          format: int32
          type: integer
        voiced:
          description: true when a voice credit names this character on this work
          type: boolean
        voices:
          items:
            $ref: "#/components/schemas/VoiceName"
          type:
            - array
            - "null"
        work:
          $ref: "#/components/schemas/WorkBrief"
      required:
        - work
        - kind
        - spoiler
        - voiced
        - voices
      type: object
    CharacterWorksResponse:
      additionalProperties: false
      properties:
        character:
          $ref: "#/components/schemas/CharacterHead"
        items:
          items:
            $ref: "#/components/schemas/CharacterWorkRow"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - character
        - items
        - total
      type: object
    ClaimActionRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/ClaimActionRequest.json
          format: uri
          readOnly: true
          type: string
        actor:
          $ref: "#/components/schemas/EditActor"
          description: The end user the product backend is acting for
        product_work_id:
          description: The product-side work id to anchor (claim only)
          format: int64
          minimum: 0
          type: integer
        reason:
          description: Moderator note; REQUIRED for decline, recorded on the event
          type: string
        site:
          description: Acting tenant; must equal the client's catalog_site binding for owner actions
          type: string
      required:
        - actor
      type: object
    ClaimActionResult:
      additionalProperties: false
      properties:
        event_id:
          format: int64
          type: integer
        from_state:
          type:
            - string
            - "null"
        to_state:
          type: string
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - from_state
        - to_state
        - event_id
      type: object
    ClaimAnchor:
      additionalProperties: false
      properties:
        external_id:
          minLength: 1
          type: string
        level:
          description: Anchor grain; defaults to work
          enum:
            - work
            - release
          type: string
        source_id:
          description: catalog_source registry id (e.g. 2=vndb 3=bangumi 4=dlsite)
          format: int32
          type: integer
      required:
        - source_id
        - external_id
      type: object
    ClaimEventFeed:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/ClaimEventFeedItem"
          type:
            - array
            - "null"
        next_since:
          format: int64
          type: integer
      required:
        - items
        - next_since
      type: object
    ClaimEventFeedItem:
      additionalProperties: false
      properties:
        actor_uid:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        from_state:
          type:
            - string
            - "null"
        id:
          format: int64
          type: integer
        product_work_id:
          description: The claim's CURRENT product-side id (a snapshot, not the value at event time)
          format: int64
          type:
            - integer
            - "null"
        reason:
          type:
            - string
            - "null"
        site:
          type: string
        to_state:
          type: string
        work_id:
          format: int64
          type: integer
      required:
        - id
        - work_id
        - from_state
        - to_state
        - actor_uid
        - reason
        - site
        - product_work_id
        - created_at
      type: object
    ClaimWorkRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/ClaimWorkRequest.json
          format: uri
          readOnly: true
          type: string
        anchors:
          description: External identity anchors; matched exact refs claim the existing registry row
          items:
            $ref: "#/components/schemas/ClaimAnchor"
          maxItems: 16
          type:
            - array
            - "null"
        content_rating:
          description: 0=all_ages 1=sensitive 2=r18
          format: int32
          maximum: 2
          minimum: 0
          type: integer
        display_name:
          minLength: 1
          type: string
        medium_id:
          description: catalog_medium registry id (1=galgame ...)
          format: int32
          type: integer
        olang:
          description: Original language (BCP-47); defaults to ja
          type: string
        product_work_id:
          description: The work's id inside the product database
          format: int64
          type: integer
        site:
          description: Claiming site key (e.g. galgame_wiki)
          minLength: 1
          type: string
      required:
        - medium_id
        - site
        - product_work_id
        - display_name
      type: object
    ClaimWorkResponse:
      additionalProperties: false
      properties:
        created:
          description: true when a new registry row was minted (no anchor matched)
          type: boolean
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - created
      type: object
    CreditGroup:
      additionalProperties: false
      properties:
        credits:
          items:
            $ref: "#/components/schemas/CreditItem"
          type:
            - array
            - "null"
        role_id:
          format: int64
          type: integer
        role_key:
          type: string
        role_name:
          type: string
      required:
        - role_id
        - role_key
        - role_name
        - credits
      type: object
    CreditItem:
      additionalProperties: false
      properties:
        character:
          type: string
        character_id:
          format: int64
          type: integer
        credit_name_id:
          format: int64
          type: integer
        lang:
          type: string
        latin:
          type: string
        name:
          type: string
        note:
          type: string
        source:
          type: string
      required:
        - credit_name_id
        - name
        - lang
      type: object
    CursorPageUserClaimItem:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/UserClaimItem"
          type:
            - array
            - "null"
        next_before:
          format: int64
          type: integer
        total:
          format: int64
          type: integer
      required:
        - items
        - next_before
        - total
      type: object
    EditActor:
      additionalProperties: false
      properties:
        is_entity_owner:
          description: Product-asserted entity ownership (owner-review overlays)
          type: boolean
        roles:
          description: The user's roles as the product's JWT asserts them
          items:
            type: string
          type:
            - array
            - "null"
        trust_tier:
          description: Trust tier TL0-TL4 (doc 18)
          format: int32
          maximum: 4
          minimum: 0
          type: integer
        user_id:
          description: Product-side user id (the shared identity space)
          format: int64
          minimum: 1
          type: integer
      required:
        - user_id
      type: object
    EditAmendRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EditAmendRequest.json
          format: uri
          readOnly: true
          type: string
        actor:
          $ref: "#/components/schemas/EditActor"
        note:
          maxLength: 2000
          type: string
        set:
          additionalProperties: {}
          description: Field-key → corrected value (change or add)
          type: object
        unset:
          description: Field keys to reject from the patch
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - actor
      type: object
    EditAmendmentView:
      additionalProperties: false
      properties:
        amender_uid:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        id:
          format: int64
          type: integer
        note:
          type: string
        seq:
          format: int64
          type: integer
        set:
          additionalProperties: {}
          type: object
        unset:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - id
        - seq
        - amender_uid
        - note
        - created_at
      type: object
    EditDecisionRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EditDecisionRequest.json
          format: uri
          readOnly: true
          type: string
        actor:
          $ref: "#/components/schemas/EditActor"
        note:
          description: Merge note / decline reason (kept on the proposal)
          maxLength: 2000
          type: string
      required:
        - actor
      type: object
    EditDiffResponse:
      additionalProperties: false
      properties:
        fields:
          items:
            $ref: "#/components/schemas/EditFieldDiffView"
          type:
            - array
            - "null"
        from_seq:
          format: int64
          type: integer
        to_seq:
          format: int64
          type: integer
      required:
        - from_seq
        - to_seq
        - fields
      type: object
    EditFieldDiffView:
      additionalProperties: false
      properties:
        diff_hint:
          type: string
        from: {}
        key:
          type: string
        kind:
          type: string
        to: {}
      required:
        - key
        - from
        - to
      type: object
    EditProposalCreateRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EditProposalCreateRequest.json
          format: uri
          readOnly: true
          type: string
        actor:
          $ref: "#/components/schemas/EditActor"
        entity_id:
          format: int64
          minimum: 1
          type: integer
        entity_type:
          description: Registered entity type, e.g. catalog.work
          minLength: 1
          type: string
        note:
          maxLength: 2000
          type: string
        patch:
          additionalProperties: {}
          description: Field-key → new-value document (registered keys only)
          type: object
        site:
          description: Filing tenant; must equal the client's catalog_site binding
          minLength: 1
          type: string
      required:
        - entity_type
        - entity_id
        - site
        - patch
        - actor
      type: object
    EditProposalCreateResponse:
      additionalProperties: false
      properties:
        merged:
          description: true when the direct-edit sugar landed the patch immediately
          type: boolean
        proposal:
          $ref: "#/components/schemas/EditProposalView"
        revision:
          $ref: "#/components/schemas/EditRevisionView"
          description: The produced revision when merged
      required:
        - proposal
        - merged
      type: object
    EditProposalListResponse:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/EditProposalView"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    EditProposalView:
      additionalProperties: false
      properties:
        amendments:
          description: Seq-ordered (detail endpoint only)
          items:
            $ref: "#/components/schemas/EditAmendmentView"
          type:
            - array
            - "null"
        base_revision_seq:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        decided_at:
          format: date-time
          type: string
        decided_by_uid:
          format: int64
          type: integer
        decision_note:
          type: string
        effective_patch:
          additionalProperties: {}
          description: patch ⊕ amendments (detail endpoint only)
          type: object
        entity_family:
          type: string
        entity_id:
          format: int64
          type: integer
        entity_type:
          type: string
        id:
          format: int64
          type: integer
        note:
          type: string
        patch:
          additionalProperties: {}
          type: object
        proposer_uid:
          format: int64
          type: integer
        site:
          type: string
        status:
          type: string
        updated_at:
          format: date-time
          type: string
      required:
        - id
        - entity_family
        - entity_type
        - entity_id
        - base_revision_seq
        - patch
        - proposer_uid
        - note
        - site
        - status
        - created_at
        - updated_at
      type: object
    EditRevertRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EditRevertRequest.json
          format: uri
          readOnly: true
          type: string
        actor:
          $ref: "#/components/schemas/EditActor"
        entity_id:
          format: int64
          minimum: 1
          type: integer
        entity_type:
          minLength: 1
          type: string
        note:
          maxLength: 2000
          type: string
        site:
          minLength: 1
          type: string
        to_seq:
          description: Target revision seq to restore
          format: int64
          minimum: 1
          type: integer
      required:
        - entity_type
        - entity_id
        - to_seq
        - site
        - actor
      type: object
    EditRevertResponse:
      additionalProperties: false
      properties:
        proposal:
          $ref: "#/components/schemas/EditProposalView"
        revision:
          $ref: "#/components/schemas/EditRevisionView"
      required:
        - proposal
        - revision
      type: object
    EditRevisionFeed:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/EditRevisionFeedItem"
          type:
            - array
            - "null"
        next_since:
          format: int64
          type: integer
      required:
        - items
        - next_since
      type: object
    EditRevisionFeedItem:
      additionalProperties: false
      properties:
        action:
          description: 0=created 1=merged 2=direct 3=reverted
          format: int32
          type: integer
        actor_uid:
          format: int64
          type: integer
        amender_uid:
          format: int64
          type:
            - integer
            - "null"
        changed_fields:
          items:
            type: string
          type:
            - array
            - "null"
        created_at:
          format: date-time
          type: string
        entity_family:
          type: string
        entity_id:
          format: int64
          type: integer
        entity_type:
          type: string
        id:
          format: int64
          type: integer
        proposal_id:
          format: int64
          type:
            - integer
            - "null"
        seq:
          format: int64
          type: integer
        site:
          type: string
      required:
        - id
        - entity_family
        - entity_type
        - entity_id
        - seq
        - action
        - changed_fields
        - actor_uid
        - amender_uid
        - proposal_id
        - site
        - created_at
      type: object
    EditRevisionListResponse:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/EditRevisionView"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    EditRevisionView:
      additionalProperties: false
      properties:
        action:
          type: string
        actor_uid:
          format: int64
          type: integer
        amender_uid:
          format: int64
          type: integer
        changed_fields:
          items:
            type: string
          type:
            - array
            - "null"
        created_at:
          format: date-time
          type: string
        entity_family:
          type: string
        entity_id:
          format: int64
          type: integer
        entity_type:
          type: string
        id:
          format: int64
          type: integer
        legacy_action:
          description: Original pre-engine action word (migrated rows only)
          type: string
        legacy_id:
          description: Source pre-engine revision row id (migrated rows only)
          format: int64
          type: integer
        legacy_minor:
          description: Old-wire minor-edit flag (migrated rows only)
          type: boolean
        legacy_note:
          description: Old-wire revision note (migrated rows only)
          type: string
        proposal_id:
          format: int64
          type: integer
        seq:
          format: int64
          type: integer
        site:
          type: string
        snapshot:
          additionalProperties: {}
          type: object
      required:
        - id
        - entity_family
        - entity_type
        - entity_id
        - seq
        - action
        - changed_fields
        - snapshot
        - actor_uid
        - site
        - created_at
      type: object
    EditSchemaFieldView:
      additionalProperties: false
      properties:
        can_propose:
          type: boolean
        can_review:
          type: boolean
        deprecated:
          type: boolean
        diff_hint:
          type: string
        key:
          type: string
        kind:
          type: string
        locked:
          type: boolean
        would_automerge:
          description: A proposal by this caller would merge instantly
          type: boolean
      required:
        - key
        - kind
        - diff_hint
        - locked
        - can_propose
        - can_review
        - would_automerge
      type: object
    EditSchemaResponse:
      additionalProperties: false
      properties:
        entity_type:
          type: string
        fields:
          items:
            $ref: "#/components/schemas/EditSchemaFieldView"
          type:
            - array
            - "null"
      required:
        - entity_type
        - fields
      type: object
    EditSnapshotResponse:
      additionalProperties: false
      properties:
        entity_id:
          format: int64
          type: integer
        entity_type:
          type: string
        values:
          additionalProperties: {}
          type: object
      required:
        - entity_type
        - entity_id
        - values
      type: object
    EditWithdrawRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EditWithdrawRequest.json
          format: uri
          readOnly: true
          type: string
        actor:
          $ref: "#/components/schemas/EditActor"
      required:
        - actor
      type: object
    EntityCounts:
      additionalProperties: false
      properties:
        characters:
          format: int64
          type: integer
        credit_names:
          format: int64
          type: integer
        labels:
          format: int64
          type: integer
        orgs:
          format: int64
          type: integer
        orphan_credit_names:
          description: credit_name.person_id IS NULL
          format: int64
          type: integer
        persons:
          format: int64
          type: integer
      required:
        - persons
        - credit_names
        - orphan_credit_names
        - orgs
        - labels
        - characters
      type: object
    EntitySearchHit:
      additionalProperties: false
      properties:
        content_rating:
          description: "works only (wave 105): 0=all_ages 1=sensitive 2=r18"
          format: int32
          type: integer
        entity_type:
          type: string
        id:
          description: "prefixed: n{id} / c{id} / b{id}"
          type: string
        kind:
          description: label kind (labels only)
          format: int32
          type: integer
        latin:
          type: string
        logo_hash:
          description: "labels only: brand logo content hash in the image service; absent = none"
          type: string
        name:
          type: string
        person_id:
          description: credit-name → person link (names only; absent = orphan)
          format: int64
          type: integer
        popularity:
          format: double
          type: number
        sources:
          items:
            type: string
          type:
            - array
            - "null"
      required:
        - id
        - entity_type
        - name
        - sources
        - popularity
      type: object
    EntitySearchResponse:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/EntitySearchHit"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    EnvelopeCatalogStats:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeCatalogStats.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/CatalogStats"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeCharacterDetailResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeCharacterDetailResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/CharacterDetailResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeCharacterWorksResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeCharacterWorksResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/CharacterWorksResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeClaimActionResult:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeClaimActionResult.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/ClaimActionResult"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeClaimEventFeed:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeClaimEventFeed.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/ClaimEventFeed"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeClaimWorkResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeClaimWorkResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/ClaimWorkResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeCursorPageUserClaimItem:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeCursorPageUserClaimItem.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/CursorPageUserClaimItem"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditAmendmentView:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditAmendmentView.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditAmendmentView"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditDiffResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditDiffResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditDiffResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditProposalCreateResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditProposalCreateResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditProposalCreateResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditProposalListResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditProposalListResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditProposalListResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditProposalView:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditProposalView.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditProposalView"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditRevertResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditRevertResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditRevertResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditRevisionFeed:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditRevisionFeed.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditRevisionFeed"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditRevisionListResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditRevisionListResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditRevisionListResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditRevisionView:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditRevisionView.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditRevisionView"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditSchemaResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditSchemaResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditSchemaResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEditSnapshotResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEditSnapshotResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EditSnapshotResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeEntitySearchResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeEntitySearchResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/EntitySearchResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeLabelWorksResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeLabelWorksResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/LabelWorksResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeNameWorksResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeNameWorksResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/NameWorksResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeRedirectFeedResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeRedirectFeedResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/RedirectFeedResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeResolveResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeResolveResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/ResolveResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeWorkByAnchorResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeWorkByAnchorResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/WorkByAnchorResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeWorkCreditsResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeWorkCreditsResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/WorkCreditsResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeWorkSearchResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeWorkSearchResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/WorkSearchResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeWorkSubmitResponse:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeWorkSubmitResponse.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/WorkSubmitResponse"
        message:
          type: string
      required:
        - code
        - message
      type: object
    HouseError:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/HouseError.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data: {}
        message:
          type: string
      required:
        - code
        - message
      type: object
    KeyCount:
      additionalProperties: false
      properties:
        count:
          format: int64
          type: integer
        key:
          type: string
      required:
        - key
        - count
      type: object
    KindCount:
      additionalProperties: false
      properties:
        count:
          format: int64
          type: integer
        kind:
          format: int32
          type: integer
      required:
        - kind
        - count
      type: object
    LabelHead:
      additionalProperties: false
      properties:
        display_name:
          type: string
        id:
          format: int64
          type: integer
        kind:
          description: label kind (0=game_brand … 4=doujin_circle …)
          format: int32
          type: integer
        logo_hash:
          description: brand logo content hash in the image service; "" = none
          type: string
      required:
        - id
        - display_name
        - kind
        - logo_hash
      type: object
    LabelWorkRow:
      additionalProperties: false
      properties:
        content_rating:
          format: int32
          type: integer
        display_name:
          type: string
        kind:
          description: attribution edge kind
          format: int32
          type: integer
        medium_id:
          format: int32
          type: integer
        status:
          format: int32
          type: integer
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - display_name
        - medium_id
        - content_rating
        - status
        - kind
      type: object
    LabelWorksResponse:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/LabelWorkRow"
          type:
            - array
            - "null"
        label:
          $ref: "#/components/schemas/LabelHead"
        total:
          format: int64
          type: integer
      required:
        - label
        - total
        - items
      type: object
    NameBuckets:
      additionalProperties: false
      properties:
        ja:
          type: string
        other:
          type: string
        zh:
          type: string
      type: object
    NameHead:
      additionalProperties: false
      properties:
        id:
          format: int64
          type: integer
        latin:
          type: string
        name:
          $ref: "#/components/schemas/NameBuckets"
        person_id:
          description: same-person group id (public links only; absent = orphan or hidden)
          format: int64
          type: integer
        siblings:
          description: the same person's other public-linked names
          items:
            $ref: "#/components/schemas/SiblingName"
          type:
            - array
            - "null"
      required:
        - id
        - name
        - siblings
      type: object
    NameWorkRole:
      additionalProperties: false
      properties:
        character:
          type: string
        character_id:
          format: int64
          type: integer
        role_id:
          format: int64
          type: integer
        role_key:
          type: string
        role_name:
          type: string
      required:
        - role_id
        - role_key
        - role_name
      type: object
    NameWorkRow:
      additionalProperties: false
      properties:
        roles:
          items:
            $ref: "#/components/schemas/NameWorkRole"
          type:
            - array
            - "null"
        work:
          $ref: "#/components/schemas/WorkBrief"
      required:
        - work
        - roles
      type: object
    NameWorksResponse:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/NameWorkRow"
          type:
            - array
            - "null"
        name:
          $ref: "#/components/schemas/NameHead"
        total:
          format: int64
          type: integer
      required:
        - name
        - items
        - total
      type: object
    QueueLevels:
      additionalProperties: false
      properties:
        candidates_by_status:
          items:
            $ref: "#/components/schemas/StatusCount"
          type:
            - array
            - "null"
        probable_refs:
          format: int64
          type: integer
        proposals_by_status:
          items:
            $ref: "#/components/schemas/StatusCount"
          type:
            - array
            - "null"
        rejections:
          format: int64
          type: integer
      required:
        - candidates_by_status
        - proposals_by_status
        - probable_refs
        - rejections
      type: object
    RedirectFeedResponse:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/RedirectItem"
          type:
            - array
            - "null"
        next_cursor:
          description: Opaque cursor for the next page; empty when this page is not full
          type: string
      required:
        - items
      type: object
    RedirectItem:
      additionalProperties: false
      properties:
        current_id:
          format: int64
          type: integer
        entity_type:
          format: int32
          type: integer
        merged_at:
          format: date-time
          type: string
        old_id:
          format: int64
          type: integer
      required:
        - entity_type
        - old_id
        - current_id
        - merged_at
      type: object
    ReleaseBrief:
      additionalProperties: false
      properties:
        anchors:
          items:
            $ref: "#/components/schemas/AnchorRef"
          type:
            - array
            - "null"
        id:
          format: int64
          type: integer
        kind:
          description: 0=default 1=digital 2=physical 3=trial 4=patch
          format: int32
          type: integer
        platform:
          type: string
        platforms:
          items:
            type: string
          type:
            - array
            - "null"
        released_d:
          format: int32
          type: integer
        released_m:
          format: int32
          type: integer
        released_y:
          format: int32
          type: integer
      required:
        - id
        - kind
        - anchors
      type: object
    ResolveRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/ResolveRequest.json
          format: uri
          readOnly: true
          type: string
        entity_type:
          description: Entity type constant (0=person 1=credit_name 2=org 3=label 4=character 5=work 6=release)
          format: int32
          maximum: 6
          minimum: 0
          type: integer
        ids:
          description: Entity ids to resolve
          items:
            format: int64
            type: integer
          maxItems: 1000
          minItems: 1
          type:
            - array
            - "null"
      required:
        - entity_type
        - ids
      type: object
    ResolveResponse:
      additionalProperties: false
      properties:
        mappings:
          additionalProperties:
            format: int64
            type: integer
          description: old id (as string key) → canonical id; unredirected ids map to themselves
          type: object
        redirected:
          description: The subset of requested ids that had a redirect
          items:
            format: int64
            type: integer
          type:
            - array
            - "null"
      required:
        - mappings
      type: object
    SiblingName:
      additionalProperties: false
      properties:
        id:
          format: int64
          type: integer
        latin:
          type: string
        name:
          $ref: "#/components/schemas/NameBuckets"
      required:
        - id
        - name
      type: object
    SourceFreshness:
      additionalProperties: false
      properties:
        latest_ref:
          description: RFC3339 max(created_at) of that source's anchors
          type: string
        source:
          type: string
      required:
        - source
      type: object
    StatusCount:
      additionalProperties: false
      properties:
        count:
          format: int64
          type: integer
        status:
          format: int32
          type: integer
      required:
        - status
        - count
      type: object
    UserClaimItem:
      additionalProperties: false
      properties:
        acted_count:
          format: int64
          type: integer
        claim_state:
          type: string
        display_name:
          type: string
        first_acted_at:
          format: date-time
          type: string
        last_actor_uid:
          format: int64
          type: integer
        last_event_at:
          format: date-time
          type: string
        last_event_id:
          format: int64
          type: integer
        last_from_state:
          type:
            - string
            - "null"
        last_reason:
          type:
            - string
            - "null"
        last_to_state:
          type: string
        product_work_id:
          format: int64
          type:
            - integer
            - "null"
        site:
          type: string
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - display_name
        - site
        - product_work_id
        - claim_state
        - last_event_id
        - last_from_state
        - last_to_state
        - last_reason
        - last_actor_uid
        - last_event_at
        - first_acted_at
        - acted_count
      type: object
    VoiceName:
      additionalProperties: false
      properties:
        credit_name_id:
          format: int64
          type: integer
        lang:
          type: string
        latin:
          type: string
        name:
          type: string
      required:
        - credit_name_id
        - name
        - lang
      type: object
    WorkBrief:
      additionalProperties: false
      properties:
        content_rating:
          description: 0=all_ages 1=sensitive 2=r18
          format: int32
          type: integer
        display_name:
          type: string
        medium_id:
          format: int32
          type: integer
        site:
          type: string
        status:
          description: 0=live 1=stub 2=merged
          format: int32
          type: integer
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - display_name
        - medium_id
        - content_rating
        - status
      type: object
    WorkByAnchorResponse:
      additionalProperties: false
      properties:
        characters:
          items:
            $ref: "#/components/schemas/WorkCharacter"
          type:
            - array
            - "null"
        covers:
          items:
            $ref: "#/components/schemas/WorkCover"
          type:
            - array
            - "null"
        intro:
          items:
            $ref: "#/components/schemas/WorkIntro"
          type:
            - array
            - "null"
        labels:
          items:
            $ref: "#/components/schemas/WorkLabel"
          type:
            - array
            - "null"
        platforms:
          items:
            $ref: "#/components/schemas/WorkPlatform"
          type:
            - array
            - "null"
        playtimes:
          items:
            $ref: "#/components/schemas/WorkPlaytime"
          type:
            - array
            - "null"
        popularity:
          items:
            $ref: "#/components/schemas/WorkPopularity"
          type:
            - array
            - "null"
        ratings:
          items:
            $ref: "#/components/schemas/WorkRating"
          type:
            - array
            - "null"
        refs:
          items:
            $ref: "#/components/schemas/WorkRef"
          type:
            - array
            - "null"
        relations:
          items:
            $ref: "#/components/schemas/WorkRelation"
          type:
            - array
            - "null"
        releases:
          items:
            $ref: "#/components/schemas/ReleaseBrief"
          type:
            - array
            - "null"
        screenshots:
          items:
            $ref: "#/components/schemas/WorkScreenshot"
          type:
            - array
            - "null"
        series:
          items:
            $ref: "#/components/schemas/WorkSeries"
          type:
            - array
            - "null"
        series_siblings:
          items:
            $ref: "#/components/schemas/WorkSeriesSibling"
          type:
            - array
            - "null"
        tags:
          items:
            $ref: "#/components/schemas/WorkTag"
          type:
            - array
            - "null"
        titles:
          items:
            $ref: "#/components/schemas/WorkTitle"
          type:
            - array
            - "null"
        work:
          $ref: "#/components/schemas/WorkCore"
      required:
        - work
        - titles
        - releases
        - labels
        - refs
        - characters
        - intro
        - covers
        - screenshots
        - ratings
        - tags
        - popularity
        - playtimes
        - series
        - platforms
        - relations
        - series_siblings
      type: object
    WorkCharacter:
      additionalProperties: false
      properties:
        character_id:
          format: int64
          type: integer
        display_name:
          type: string
        figure_hash:
          type: string
        gender:
          format: int32
          type: integer
        image_hash:
          type: string
        kind:
          description: "appearance strength: 0=unknown 1=main 2=secondary 3=appears"
          format: int32
          type: integer
        latin:
          type: string
        spoiler:
          description: "appearance spoiler level: 0=none 1=minor 2=major"
          format: int32
          type: integer
        va:
          items:
            $ref: "#/components/schemas/WorkCharacterVA"
          type:
            - array
            - "null"
      required:
        - character_id
        - display_name
        - kind
        - spoiler
        - va
      type: object
    WorkCharacterVA:
      additionalProperties: false
      properties:
        credit_name_id:
          format: int64
          type: integer
        name:
          type: string
      required:
        - credit_name_id
        - name
      type: object
    WorkCore:
      additionalProperties: false
      properties:
        content_rating:
          description: 0=all_ages 1=sensitive 2=r18
          format: int32
          type: integer
        display_name:
          type: string
        id:
          format: int64
          type: integer
        medium_id:
          format: int32
          type: integer
        olang:
          type: string
        product_work_id:
          format: int64
          type: integer
        site:
          type: string
        status:
          description: 0=live 1=stub 2=merged
          format: int32
          type: integer
      required:
        - id
        - medium_id
        - display_name
        - olang
        - content_rating
        - status
      type: object
    WorkCover:
      additionalProperties: false
      properties:
        image_hash:
          type: string
        kind:
          type: string
        portrait_pinned:
          description: true = the vertical portrait pin (portrait-first UI)
          type: boolean
        sexual:
          description: "content flag: 0=safe 1=suggestive 2=explicit"
          format: int32
          type: integer
        sort_order:
          description: gallery/pin position; 0 = pinned landscape banner
          format: int64
          type: integer
        source_id:
          description: "catalog_source id (provenance): galgame_wiki/vndb/bangumi/upscale for a bridged claimed cover, the backfill source for a bodyless cover"
          format: int32
          type: integer
        violence:
          description: "content flag: 0=safe 1=suggestive 2=explicit"
          format: int32
          type: integer
      required:
        - image_hash
        - kind
        - portrait_pinned
        - sort_order
        - sexual
        - violence
        - source_id
      type: object
    WorkCreditsResponse:
      additionalProperties: false
      properties:
        groups:
          items:
            $ref: "#/components/schemas/CreditGroup"
          type:
            - array
            - "null"
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - groups
      type: object
    WorkIntro:
      additionalProperties: false
      properties:
        intro:
          type: string
        lang:
          type: string
        machine:
          description: true if this intro is an LLM machine translation (no source text for this language); omitted for source/bridged rows
          type: boolean
        source_id:
          description: "catalog_source id (provenance): e.g. galgame_wiki for a bridged claimed work, vndb for a bodyless backfill"
          format: int32
          type: integer
      required:
        - lang
        - intro
        - source_id
      type: object
    WorkLabel:
      additionalProperties: false
      properties:
        display_name:
          type: string
        kind:
          description: "attribution nature: 0=circle 1=publisher 2=developer 3=brand"
          format: int32
          type: integer
        label_id:
          format: int64
          type: integer
        label_kind:
          description: the label's own kind (0=game_brand … 4=doujin_circle …)
          format: int32
          type: integer
      required:
        - label_id
        - display_name
        - label_kind
        - kind
      type: object
    WorkPlatform:
      additionalProperties: false
      properties:
        platform:
          description: catalog_platform registry code (win/and/ios/…)
          type: string
        source_id:
          description: catalog_source id that asserted it
          format: int32
          type: integer
      required:
        - platform
        - source_id
      type: object
    WorkPlaytime:
      additionalProperties: false
      properties:
        minutes:
          description: median playtime in minutes (unit-normalized; estimate semantics stay source-native)
          format: int64
          type: integer
        source_id:
          description: "catalog_source id (provenance): vndb = vote-backed median, erogamespace = community median"
          format: int32
          type: integer
        vote_count:
          description: user reports backing the estimate; 0 = the source publishes no per-work count
          format: int64
          type: integer
      required:
        - source_id
        - minutes
        - vote_count
      type: object
    WorkPopularity:
      additionalProperties: false
      properties:
        metric:
          description: "popularity metric: 0=downloads 1=wishlist 2=reviews (extensible vocabulary)"
          format: int32
          type: integer
        source_id:
          description: "catalog_source id (provenance + unit selector): counters are source-relative and never summed across sources"
          format: int32
          type: integer
        value:
          description: raw counter verbatim from the source; a published 0 is a real value, an unpublished counter has no element
          format: int64
          type: integer
      required:
        - source_id
        - metric
        - value
      type: object
    WorkRating:
      additionalProperties: false
      properties:
        rank:
          description: source-internal rank; absent when the source has no rank or the work is unranked
          format: int64
          type: integer
        score:
          description: rating on the source-native scale (never normalized across sources)
          format: double
          type: number
        source_id:
          description: "catalog_source id (provenance + scale selector): vndb = 1-10 mean, bangumi = 0-10 mean, dlsite = 0-5 star mean, erogamespace = 0-100 median"
          format: int32
          type: integer
        vote_count:
          description: number of ratings backing the score
          format: int64
          type: integer
      required:
        - source_id
        - score
        - vote_count
      type: object
    WorkRef:
      additionalProperties: false
      properties:
        external_id:
          type: string
        level:
          description: work | release
          type: string
        release_id:
          description: present when level=release
          format: int64
          type: integer
        source:
          type: string
      required:
        - source
        - external_id
        - level
      type: object
    WorkRelation:
      additionalProperties: false
      properties:
        content_rating:
          format: int32
          type: integer
        display_name:
          type: string
        medium_id:
          format: int32
          type: integer
        phrase:
          description: direction-resolved display phrase from this work's perspective
          type: string
        product_work_id:
          format: int64
          type: integer
        relation_type:
          description: vocabulary key, e.g. sequel_of / fandisc_of / remake_of / collects / shares_character
          type: string
        site:
          description: claim site when the other end is claimed
          type: string
        status:
          format: int32
          type: integer
        work_id:
          description: the other end's catalog work id
          format: int64
          type: integer
      required:
        - relation_type
        - phrase
        - work_id
        - display_name
        - medium_id
        - content_rating
        - status
      type: object
    WorkScreenshot:
      additionalProperties: false
      properties:
        caption:
          type: string
        image_hash:
          type: string
        sexual:
          description: "content flag: 0=safe 1=suggestive 2=explicit"
          format: int32
          type: integer
        sort_order:
          description: gallery position
          format: int64
          type: integer
        source_id:
          description: "catalog_source id (provenance): galgame_wiki/vndb for a bridged claimed screenshot, the backfill source for a bodyless screenshot"
          format: int32
          type: integer
        violence:
          description: "content flag: 0=safe 1=suggestive 2=explicit"
          format: int32
          type: integer
      required:
        - image_hash
        - caption
        - sort_order
        - sexual
        - violence
        - source_id
      type: object
    WorkSearchHit:
      additionalProperties: false
      properties:
        content_rating:
          description: 0=all_ages 1=sensitive 2=r18
          format: int32
          type: integer
        display_name:
          type: string
        dlsite_id:
          type: string
        medium_id:
          format: int32
          type: integer
        site:
          type: string
        status:
          description: 0=live 1=stub 2=merged
          format: int32
          type: integer
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - display_name
        - medium_id
        - content_rating
        - status
      type: object
    WorkSearchResponse:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/WorkSearchHit"
          type:
            - array
            - "null"
      required:
        - items
      type: object
    WorkSeries:
      additionalProperties: false
      properties:
        id:
          format: int64
          type: integer
        member_count:
          description: total anchored works in this series
          format: int64
          type: integer
        name:
          description: series display name, verbatim from the source (dlsite series_name)
          type: string
        source_id:
          description: catalog_source id the series was materialized from
          format: int32
          type: integer
      required:
        - id
        - name
        - source_id
        - member_count
      type: object
    WorkSeriesSibling:
      additionalProperties: false
      properties:
        content_rating:
          format: int32
          type: integer
        display_name:
          type: string
        medium_id:
          format: int32
          type: integer
        product_work_id:
          format: int64
          type: integer
        site:
          type: string
        status:
          format: int32
          type: integer
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - display_name
        - medium_id
        - content_rating
        - status
      type: object
    WorkSubmitDate:
      additionalProperties: false
      properties:
        d:
          description: 0 = unknown day; requires m
          format: int32
          maximum: 31
          minimum: 0
          type: integer
        m:
          description: 0 = unknown month
          format: int32
          maximum: 12
          minimum: 0
          type: integer
        "y":
          format: int32
          maximum: 2200
          minimum: 1970
          type: integer
      required:
        - "y"
      type: object
    WorkSubmitRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/WorkSubmitRequest.json
          format: uri
          readOnly: true
          type: string
        actor:
          $ref: "#/components/schemas/EditActor"
          description: The end user the product backend is submitting for
        fields:
          additionalProperties: {}
          description: "Field-key → value, the submission subset of catalog.work: display_name (required), olang, content_rating, titles, intros, display_nsfw, tag_ids, labels, engine_ids, series_ids, links. covers/screenshots are NOT accepted here — upload the bytes, then edit those facets"
          type: object
        product_work_id:
          description: "The product-side work id to anchor this submission at. OMIT IT to have the registry issue the identity: the claim then adopts the minted work id, which the response returns. Idempotency depends on this choice — see the endpoint summary"
          format: int64
          minimum: 0
          type: integer
        released:
          $ref: "#/components/schemas/WorkSubmitDate"
          description: Optional submitted release date; becomes ONE curated catalog_release row. Omit for TBA
        site:
          description: Submitting tenant; must equal the client's catalog_site binding
          minLength: 1
          type: string
      required:
        - site
        - actor
        - fields
      type: object
    WorkSubmitResponse:
      additionalProperties: false
      properties:
        claim_state:
          description: Always pending — a submission is born awaiting review
          type: string
        event_id:
          description: The claim-event row recording the birth (from_state null → pending)
          format: int64
          type: integer
        product_work_id:
          format: int64
          type: integer
        release_id:
          description: The curated release row the submitted date produced; absent when no date was given
          format: int64
          type: integer
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - product_work_id
        - claim_state
        - event_id
      type: object
    WorkTag:
      additionalProperties: false
      properties:
        canonical_id:
          description: catalog_tag id when this (source_id, name) is mapped into the canonical vocabulary; absent otherwise
          format: int64
          type: integer
        count:
          description: source vote count backing the tag; absent when the source has no votes (bridged galgame wiki tags)
          format: int64
          type: integer
        kind:
          description: canonical kind (0=content 1=meta/platform-attribute); absent when unmapped
          format: int32
          type: integer
        name:
          description: tag text, verbatim from the source (no vocabulary mapping)
          type: string
        source_id:
          description: "catalog_source id (provenance): galgame_wiki/vndb for a bridged claimed tag, the backfill source (bangumi) for a bodyless tag"
          format: int32
          type: integer
        tier:
          description: canonical display tier (0=core 1=longtail 2=hidden); absent when unmapped
          format: int32
          type: integer
      required:
        - name
        - source_id
      type: object
    WorkTitle:
      additionalProperties: false
      properties:
        kind:
          description: 0=official 1=alias 2=abbreviation 3=search_hint
          format: int32
          type: integer
        lang:
          type: string
        latin:
          type: string
        title:
          type: string
      required:
        - lang
        - title
        - kind
      type: object
    WorksCell:
      additionalProperties: false
      properties:
        claimed:
          type: boolean
        count:
          format: int64
          type: integer
        medium_id:
          format: int32
          type: integer
        status:
          description: 0=live 1=stub 2=merged
          format: int32
          type: integer
      required:
        - medium_id
        - claimed
        - status
        - count
      type: object
    WorksMatrix:
      additionalProperties: false
      properties:
        cells:
          items:
            $ref: "#/components/schemas/WorksCell"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - total
        - cells
      type: object
info:
  title: KUN Catalog Service
  version: 1.0.0
openapi: 3.1.0
paths:
  /api/v1/catalog/characters/{id}:
    get:
      operationId: getCatalogCharacterByID
      parameters:
        - description: Catalog character id
          in: path
          name: id
          required: true
          schema:
            description: Catalog character id
            format: int64
            type: integer
        - description: max trait spoiler level to include (0-2, default 0)
          explode: false
          in: query
          name: spoilers
          schema:
            description: max trait spoiler level to include (0-2, default 0)
            format: int32
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeCharacterDetailResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Read a character's identity + aliases by catalog id
      tags:
        - catalog-s2s
  /api/v1/catalog/characters/{id}/works:
    get:
      operationId: getCatalogCharacterWorks
      parameters:
        - description: Catalog character id
          in: path
          name: id
          required: true
          schema:
            description: Catalog character id
            format: int64
            type: integer
        - description: Page size (capped at 50)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: Page size (capped at 50)
            format: int64
            type: integer
        - description: Rows to skip
          explode: false
          in: query
          name: offset
          schema:
            description: Rows to skip
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeCharacterWorksResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Works a character appears in (roster edges ∪ voice credits) with kind + voice names
      tags:
        - catalog-s2s
  /api/v1/catalog/claim-events/feed:
    get:
      operationId: listCatalogClaimEvents
      parameters:
        - description: "Exclusive cursor: return events with a greater id (0 = from the beginning)"
          explode: false
          in: query
          name: since
          schema:
            description: "Exclusive cursor: return events with a greater id (0 = from the beginning)"
            format: int64
            type: integer
        - description: Page size (default 200, max 1000)
          explode: false
          in: query
          name: limit
          schema:
            description: Page size (default 200, max 1000)
            format: int64
            type: integer
        - description: Restrict to one claiming site
          explode: false
          in: query
          name: site
          schema:
            description: Restrict to one claiming site
            type: string
        - description: Restrict to transitions caused by this user (0 = every actor)
          explode: false
          in: query
          name: actor_uid
          schema:
            description: Restrict to transitions caused by this user (0 = every actor)
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeClaimEventFeed"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Cursor feed of claim-state transitions (ascending id; the source for downstream inboxes and point awards)
      tags:
        - catalog-lifecycle
  /api/v1/catalog/edit-revisions/feed:
    get:
      operationId: listCatalogEditRevisions
      parameters:
        - description: "Exclusive cursor: return revisions with a greater id (0 = from the beginning)"
          explode: false
          in: query
          name: since
          schema:
            description: "Exclusive cursor: return revisions with a greater id (0 = from the beginning)"
            format: int64
            type: integer
        - description: Page size (default 200, max 1000)
          explode: false
          in: query
          name: limit
          schema:
            description: Page size (default 200, max 1000)
            format: int64
            type: integer
        - description: Restrict to one family, e.g. catalog
          explode: false
          in: query
          name: entity_family
          schema:
            description: Restrict to one family, e.g. catalog
            type: string
        - description: Restrict to one type, e.g. catalog.work
          explode: false
          in: query
          name: entity_type
          schema:
            description: Restrict to one type, e.g. catalog.work
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditRevisionFeed"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Cursor feed of editing-engine revisions across all entities (ascending id; filterable by entity family/type)
      tags:
        - catalog-lifecycle
  /api/v1/catalog/edit/diff:
    get:
      operationId: diffEditRevisions
      parameters:
        - explode: false
          in: query
          name: entity_type
          schema:
            minLength: 1
            type: string
        - explode: false
          in: query
          name: entity_id
          schema:
            format: int64
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: from_seq
          schema:
            format: int64
            minimum: 1
            type: integer
        - explode: false
          in: query
          name: to_seq
          schema:
            format: int64
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditDiffResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Field-level diff between any two revisions
      tags:
        - catalog-edit
  /api/v1/catalog/edit/proposals:
    get:
      operationId: listEditProposals
      parameters:
        - description: Filter to one entity type
          explode: false
          in: query
          name: entity_type
          schema:
            description: Filter to one entity type
            type: string
        - description: Filter to one entity (requires entity_type)
          explode: false
          in: query
          name: entity_id
          schema:
            description: Filter to one entity (requires entity_type)
            format: int64
            type: integer
        - description: Filter to one tenant
          explode: false
          in: query
          name: site
          schema:
            description: Filter to one tenant
            type: string
        - description: Filter to one proposer (the BFF 'my proposals' face); 0 = all
          explode: false
          in: query
          name: proposer_uid
          schema:
            description: Filter to one proposer (the BFF 'my proposals' face); 0 = all
            format: int64
            type: integer
        - description: Filter by status; empty = all
          explode: false
          in: query
          name: status
          schema:
            description: Filter by status; empty = all
            enum:
              - ""
              - open
              - merged
              - declined
              - withdrawn
            type: string
        - description: Page size (max 200, default 50)
          explode: false
          in: query
          name: limit
          schema:
            description: Page size (max 200, default 50)
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditProposalListResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: List edit proposals (review queue)
      tags:
        - catalog-edit
    post:
      operationId: createEditProposal
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditProposalCreateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditProposalCreateResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: File an edit proposal (automerges into a direct edit when policy allows)
      tags:
        - catalog-edit
  /api/v1/catalog/edit/proposals/{id}:
    get:
      operationId: getEditProposal
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditProposalView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Read one proposal with amendments and the effective patch
      tags:
        - catalog-edit
  /api/v1/catalog/edit/proposals/{id}/amendments:
    post:
      operationId: amendEditProposal
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditAmendRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditAmendmentView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Amend an open proposal (set/unset fields; requires the review rule)
      tags:
        - catalog-edit
  /api/v1/catalog/edit/proposals/{id}/decline:
    post:
      operationId: declineEditProposal
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditDecisionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditProposalView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Decline an open proposal with a reason
      tags:
        - catalog-edit
  /api/v1/catalog/edit/proposals/{id}/merge:
    post:
      operationId: mergeEditProposal
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditDecisionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditRevisionView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Merge an open proposal (per-field rebase; 409 lists conflicts)
      tags:
        - catalog-edit
  /api/v1/catalog/edit/proposals/{id}/withdraw:
    post:
      operationId: withdrawEditProposal
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditWithdrawRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditProposalView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Withdraw one's own open proposal
      tags:
        - catalog-edit
  /api/v1/catalog/edit/revert:
    post:
      operationId: revertEditEntity
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditRevertRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditRevertResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Restore an entity to a historical revision (a new revision; history kept)
      tags:
        - catalog-edit
  /api/v1/catalog/edit/revisions:
    get:
      operationId: listEditRevisions
      parameters:
        - description: Registered entity type, e.g. catalog.work
          explode: false
          in: query
          name: entity_type
          schema:
            description: Registered entity type, e.g. catalog.work
            minLength: 1
            type: string
        - explode: false
          in: query
          name: entity_id
          schema:
            format: int64
            minimum: 1
            type: integer
        - description: Page size (max 200, default 50)
          explode: false
          in: query
          name: limit
          schema:
            description: Page size (max 200, default 50)
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditRevisionListResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: An entity's revision log, newest-first
      tags:
        - catalog-edit
  /api/v1/catalog/edit/schema/{entity_type}:
    get:
      operationId: getEditSchema
      parameters:
        - description: Registered entity type, e.g. catalog.work
          in: path
          name: entity_type
          required: true
          schema:
            description: Registered entity type, e.g. catalog.work
            type: string
        - description: "Entity-aware projection: owner automerge evaluates against this entity (0 = type-level, owner projects false)"
          explode: false
          in: query
          name: entity_id
          schema:
            description: "Entity-aware projection: owner automerge evaluates against this entity (0 = type-level, owner projects false)"
            format: int64
            type: integer
        - description: Tenant whose policy overlay applies
          explode: false
          in: query
          name: site
          schema:
            description: Tenant whose policy overlay applies
            type: string
        - description: Asserted end-user id (0 = anonymous projection)
          explode: false
          in: query
          name: user_id
          schema:
            description: Asserted end-user id (0 = anonymous projection)
            format: int64
            type: integer
        - description: Comma-separated asserted roles
          explode: false
          in: query
          name: roles
          schema:
            description: Comma-separated asserted roles
            type: string
        - description: Asserted trust tier
          explode: false
          in: query
          name: trust_tier
          schema:
            description: Asserted trust tier
            format: int32
            maximum: 4
            minimum: 0
            type: integer
        - description: Product-asserted ownership of entity_id (owner-review overlays project can_review)
          explode: false
          in: query
          name: is_entity_owner
          schema:
            description: Product-asserted ownership of entity_id (owner-review overlays project can_review)
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditSchemaResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Field schema + the caller's evaluated field-level capabilities
      tags:
        - catalog-edit
  /api/v1/catalog/edit/snapshot:
    get:
      operationId: getEditSnapshot
      parameters:
        - description: Registered entity type, e.g. galgame.game
          explode: false
          in: query
          name: entity_type
          schema:
            description: Registered entity type, e.g. galgame.game
            minLength: 1
            type: string
        - explode: false
          in: query
          name: entity_id
          schema:
            format: int64
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEditSnapshotResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: The entity's current registered-field values (the BFF editor's bootstrap read)
      tags:
        - catalog-edit
  /api/v1/catalog/labels/{id}/works:
    get:
      operationId: getCatalogLabelWorks
      parameters:
        - description: Catalog label id
          in: path
          name: id
          required: true
          schema:
            description: Catalog label id
            format: int64
            type: integer
        - description: Page size (capped at 50)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: Page size (capped at 50)
            format: int64
            type: integer
        - description: Rows to skip
          explode: false
          in: query
          name: offset
          schema:
            description: Rows to skip
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeLabelWorksResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Works attributed to a label (circle→works reverse, paginated)
      tags:
        - catalog-s2s
  /api/v1/catalog/names/{id}/works:
    get:
      operationId: getCatalogNameWorks
      parameters:
        - description: Catalog credit-name id
          in: path
          name: id
          required: true
          schema:
            description: Catalog credit-name id
            format: int64
            type: integer
        - description: Page size (capped at 50)
          explode: false
          in: query
          name: limit
          schema:
            default: 50
            description: Page size (capped at 50)
            format: int64
            type: integer
        - description: Rows to skip
          explode: false
          in: query
          name: offset
          schema:
            description: Rows to skip
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeNameWorksResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Works a credited name worked on (name→works reverse; sibling names + roles)
      tags:
        - catalog-s2s
  /api/v1/catalog/redirects:
    get:
      operationId: listCatalogRedirects
      parameters:
        - description: Filter to one entity type; -1 = all
          explode: false
          in: query
          name: entity_type
          schema:
            default: -1
            description: Filter to one entity type; -1 = all
            format: int32
            type: integer
        - description: Opaque cursor from the previous page's next_cursor
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque cursor from the previous page's next_cursor
            type: string
        - description: Page size (max 1000)
          explode: false
          in: query
          name: limit
          schema:
            description: Page size (max 1000)
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeRedirectFeedResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Keyset feed of redirects for product-site id cleanup crons
      tags:
        - catalog-s2s
  /api/v1/catalog/resolve:
    post:
      operationId: resolveCatalogIDs
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeResolveResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Resolve entity ids to their canonical ids (redirect following, batch)
      tags:
        - catalog-s2s
  /api/v1/catalog/search/entities:
    get:
      operationId: searchCatalogEntities
      parameters:
        - description: Search text; empty returns the most-credited entities
          explode: false
          in: query
          name: q
          schema:
            description: Search text; empty returns the most-credited entities
            type: string
        - description: "Which entity index to search (works: wave 105 — LIVE galgame registry works, r18 verbatim on this face)"
          explode: false
          in: query
          name: type
          schema:
            description: "Which entity index to search (works: wave 105 — LIVE galgame registry works, r18 verbatim on this face)"
            enum:
              - names
              - characters
              - labels
              - works
            type: string
        - description: UI locale; the server pins the query language (client-supplied Meili locales are never accepted)
          explode: false
          in: query
          name: locale
          schema:
            description: UI locale; the server pins the query language (client-supplied Meili locales are never accepted)
            enum:
              - zh
              - ja
              - en
            type: string
        - description: Max hits (capped at 20)
          explode: false
          in: query
          name: limit
          schema:
            default: 20
            description: Max hits (capped at 20)
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeEntitySearchResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Search catalog entities (credit names / characters / labels)
      tags:
        - catalog-s2s
  /api/v1/catalog/stats:
    get:
      operationId: getCatalogStats
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeCatalogStats"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Dashboard counts for the internal data browser (one round-trip)
      tags:
        - catalog-s2s
  /api/v1/catalog/users/{uid}/claims:
    get:
      operationId: listCatalogClaimsByUser
      parameters:
        - description: The product-side user id recorded as the event actor
          in: path
          name: uid
          required: true
          schema:
            description: The product-side user id recorded as the event actor
            format: int64
            minimum: 1
            type: integer
        - description: Restrict to one claiming site
          explode: false
          in: query
          name: site
          schema:
            description: Restrict to one claiming site
            type: string
        - description: Comma-separated subset of none, live, draft, pending, declined, hidden; absent = every state
          explode: false
          in: query
          name: claim_state
          schema:
            description: Comma-separated subset of none, live, draft, pending, declined, hidden; absent = every state
            type: string
        - description: "Exclusive cursor: return works whose last_event_id is smaller (0 = first page)"
          explode: false
          in: query
          name: before
          schema:
            description: "Exclusive cursor: return works whose last_event_id is smaller (0 = first page)"
            format: int64
            type: integer
        - description: Page size (default 20, max 100)
          explode: false
          in: query
          name: limit
          schema:
            description: Page size (default 20, max 100)
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeCursorPageUserClaimItem"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: "The claims a user has acted on: current state, latest transition and reason, most recent activity first (cursor: before=last_event_id)"
      tags:
        - catalog-lifecycle
  /api/v1/catalog/works/by-anchor:
    get:
      operationId: getCatalogWorkByAnchor
      parameters:
        - description: Source key (dlsite/vndb/bangumi/erogamespace/…), validated against the source registry
          explode: false
          in: query
          name: source
          schema:
            description: Source key (dlsite/vndb/bangumi/erogamespace/…), validated against the source registry
            minLength: 1
            type: string
        - description: The id within that source (e.g. a DLsite RJ number, a VNDB v-id)
          explode: false
          in: query
          name: external_id
          schema:
            description: The id within that source (e.g. a DLsite RJ number, a VNDB v-id)
            minLength: 1
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeWorkByAnchorResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Read a work through one of its external anchors (work- or release-level)
      tags:
        - catalog-s2s
  /api/v1/catalog/works/claim:
    post:
      operationId: claimCatalogWork
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimWorkRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeClaimWorkResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Claim (or register) the catalog work for a product-side work
      tags:
        - catalog-s2s
  /api/v1/catalog/works/search:
    get:
      operationId: searchCatalogWorks
      parameters:
        - description: Title search text (NFKC-folded substring match)
          explode: false
          in: query
          name: q
          schema:
            description: Title search text (NFKC-folded substring match)
            minLength: 1
            type: string
        - description: Filter to one medium; -1 = all
          explode: false
          in: query
          name: medium_id
          schema:
            default: -1
            description: Filter to one medium; -1 = all
            format: int32
            type: integer
        - description: Max hits (capped at 50)
          explode: false
          in: query
          name: limit
          schema:
            default: 20
            description: Max hits (capped at 50)
            format: int64
            type: integer
        - description: Comma-separated subset of none, live, draft, pending, declined, hidden; absent = every state
          explode: false
          in: query
          name: claim_state
          schema:
            description: Comma-separated subset of none, live, draft, pending, declined, hidden; absent = every state
            type: string
        - description: Restrict to works claimed by ONE site; absent = every tenant and every unclaimed work. Live SQL predicate — no reindex delay
          explode: false
          in: query
          name: site
          schema:
            description: Restrict to works claimed by ONE site; absent = every tenant and every unclaimed work. Live SQL predicate — no reindex delay
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeWorkSearchResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Search works by title (NFKC-folded substring) for the product-side create picker
      tags:
        - catalog-s2s
  /api/v1/catalog/works/submit:
    post:
      operationId: submitCatalogWork
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorkSubmitRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeWorkSubmitResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: "Mint a work in the pending claim state from a submission form (one transaction: registry row + content + birth event). product_work_id is OPTIONAL: omit it and the registry issues the identity, the claim adopting the minted work id (returned as product_work_id — create your local row at it). IDEMPOTENCY: with product_work_id, a repeat is a 409 echoing the existing work (matched_by=claim); without it, a repeat is recognized only by the identity anchors the payload's links assert (matched_by=anchor, scoped to your own site) — a submission that omits BOTH the id and any VNDB/Bangumi link has no key to match on and WILL mint a second work if retried"
      tags:
        - catalog-lifecycle
  /api/v1/catalog/works/{id}:
    get:
      operationId: getCatalogWorkByID
      parameters:
        - description: Catalog work id
          in: path
          name: id
          required: true
          schema:
            description: Catalog work id
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeWorkByAnchorResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Read a work by catalog id (same bundle as by-anchor)
      tags:
        - catalog-s2s
  /api/v1/catalog/works/{id}/claim-actions/{action}:
    post:
      operationId: actOnCatalogClaim
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            minimum: 1
            type: integer
        - description: claim | submit | publish | withdraw | approve | decline | ban | unban
          in: path
          name: action
          required: true
          schema:
            description: claim | submit | publish | withdraw | approve | decline | ban | unban
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClaimActionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeClaimActionResult"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: "Move a claim through its lifecycle: claim / submit / publish / withdraw (owner) or approve / decline / ban / unban (review). 409 on an illegal transition, echoing the current state"
      tags:
        - catalog-lifecycle
  /api/v1/catalog/works/{id}/credits:
    get:
      operationId: getCatalogWorkCredits
      parameters:
        - description: Catalog work id
          in: path
          name: id
          required: true
          schema:
            description: Catalog work id
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeWorkCreditsResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: List a work's credits grouped by role
      tags:
        - catalog-s2s
