# Build context is the repo root for every image, so be aggressive — without
# this Docker ships gigabytes of node_modules / build output to the daemon.
.git
.github
**/node_modules
**/.nuxt
**/.output
**/dist
apps/api/tmp
apps/api/build
apps/api/**/*.test
**/*.log
**/.DS_Store

# Local env / secrets — never bake into an image (docker/*.env is read at
# runtime via env_file, not copied in).
**/.env
**/.env.*
docker/*.env

# Big local fixtures / dumps not needed in any image.
scripts/*.dump
**/coverage
**/.pnpm-store

# Docs / references — not needed to build.
docs
# …except tagMap.ts: sync-vndb / sync-vndb-relations parse it at runtime, so the
# tools image must be able to COPY it (re-include the one file under docs/).
!docs/tagMap.ts
**/refs
